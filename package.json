{
  "name": "food-delivery",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:types": "pnpm --filter @food-delivery/types build",
    "watch:types": "pnpm --filter @food-delivery/types watch",
    "dev:backend": "pnpm --filter backend run start:dev",
    "dev:frontend": "pnpm --filter frontend run dev",
    "dev": "pnpm build:types && concurrently \"pnpm dev:backend\" \"pnpm dev:frontend\" \"pnpm watch:types\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.1.3",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
